{
  "name": "zk-credentials-circuits",
  "version": "1.0.0",
  "description": "Circom ZK circuits for income proof generation",
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "generate-verifier": "bash scripts/generate_verifier.sh",
    "test": "node test/generate_proof.js",
    "build": "npm run compile && npm run setup && npm run generate-verifier"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.0",
    "circomlibjs": "^0.1.7"
  },
  "devDependencies": {
    "circom2": "^0.2.19"
  }
}
